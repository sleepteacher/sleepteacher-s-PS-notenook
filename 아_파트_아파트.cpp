#include <bits/stdc++.h>

using namespace std;

int main() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);
	int i, j, k, l, N, M, temp, a, b,jug[3][3];
	string a;
	char arr[3][3] = {{'k', 'e', 'm'}, {'a', 'p', 'o'}, {'r', 'b', 'g'}};
	cin >> N >> M;
	for (i = 0; i < N; i++) {
		int loop[3] = {0,};
		cin >> a;
		for(j = 0; j < 3; j++){
			if (a[0] = arr[i][j]){

			}
		}
	}

	return 0;
}