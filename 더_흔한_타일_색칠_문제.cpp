#include <bits/stdc++.h>
using namespace std;

string arr[500];
map<string, int> M;

int main() {
	cin.tie(NULL);
	cout.tie(NULL);
	ios::sync_with_stdio(false);
	int i, j, k, l, N, K, M, a, b, c;
	cin >> N >> M >> K;
	for(i = 0; i < N; i++){
		
	}

	return 0;
}
