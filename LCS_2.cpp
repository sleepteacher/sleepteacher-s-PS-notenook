#include <bits/stdc++.h>

using namespace std;

int main(){
    cin.tie(NULL);
    cout.tie(NULL);
    ios::sync_with_stdio(false);
    int i,j,k,l;
    int arr[26][1001];

    for(i = 0; i < 26; i++){
        fill(&arr[i][0], &arr[i][1000], -1);
    }
    
    

    return 0;
}